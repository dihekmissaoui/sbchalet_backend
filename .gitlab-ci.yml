image: dihekmissaoui/devops
stages:
    - build
    - test

cache:
  paths:
    - .m2/repository
    - target

build_job:
  stage: build
  script: 
    - echo "Maven compile started"
    - "mvn compile"


test_job:
  stage: test
  script: 
    - echo "Maven test started"
    - "mvn test"


